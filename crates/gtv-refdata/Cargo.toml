[package]
name = "gtv-refdata"
version.workspace = true
edition.workspace = true
license.workspace = true

# prod_p4 D6 (P2.6): legal-entity / organisation / product hierarchies, master
# data and reference data with effective dating.
#
# Enterprise-batch boundary: pure domain layer. It does not depend on
# gtv-engine / gtv-index / gtv-pattern, and those crates must never depend on
# it (see doc/prod_p4_design.md §1–§3).

[dependencies]
gtv-core = { path = "../gtv-core" }
thiserror.workspace = true
